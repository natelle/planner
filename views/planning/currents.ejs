<%- include ../header.ejs %>

<nav aria-label="breadcrumb">
  <ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="/"><%= __("global.home") %></a></li>
    <li class="breadcrumb-item"><a href="/planning"><%= __("planning.menu") %></a></li>
    <li class="breadcrumb-item"><%= __("planning.currents") %></li>
  </ol>
</nav>


<%
    var now = new Date();
    var month = (now.getMonth()+1).toString().padStart(2,0);
    var year = now.getFullYear();

    for(var category of categories) {
        if(category) {
%>
            <h4><a href="/planning/category/<%= category.id %>/<%= month + year %>"><%= category.name %></a></h4>
        <% } else { %>
            <h4><a href="/planning/category/0/<%= month + year %>"><%= __("category.default") %></a></h4>
        <% } %>
        <br>
     <% } %>

<%- include ../footer.ejs %>
